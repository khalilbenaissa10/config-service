server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/khalilbenaissa10/myConfig
      overrideSystemProperties: false
      overrideNone: true